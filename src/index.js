import ElInputNumberOnly from './el-Input-number-only/index.vue';

ElInputNumberOnly.install = (Vue) => {
  Vue.component(ElInputNumberOnly.name, ElInputNumberOnly);
};

export default ElInputNumberOnly;
